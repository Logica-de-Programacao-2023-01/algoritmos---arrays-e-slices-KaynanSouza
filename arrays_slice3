package main

import "fmt"

func main() {

	produto := 1.0

	var numero = [4]float64{1.2, 1.5, 4.6, 3.4}

	for i := 0; i < len(numero); i++ {
		produto = numero[i] * produto
	}
	fmt.Println(produto)
}
